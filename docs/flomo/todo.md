API doc is `docs/api/flomo.md`.

Common components from shadcn are in `client/src/components/ui/`. Use `lucide` icons.

Flomo shares the same `client/src/index.css` with Dashboard. Follow the UI styles in `client/src/components/todo/todo-list.tsx` and `client/src/components/todo/todo-entry.tsx`. But use global dialogs instead of dialogs per list element like todos.

## TODO: tiny fixes

1. sync interval from 5s to 10s to 20s. Every update local or remote cause interval to change back to 5s.
2.

## TODO: Build UI Components
