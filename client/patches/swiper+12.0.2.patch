diff --git a/node_modules/swiper/modules/zoom.mjs b/node_modules/swiper/modules/zoom.mjs
index ecd1a51..b433895 100644
--- a/node_modules/swiper/modules/zoom.mjs
+++ b/node_modules/swiper/modules/zoom.mjs
@@ -450,6 +450,11 @@ function Zoom({
       }
     }
     if (!gesture.imageEl || !gesture.imageWrapEl) return;
+
+    // PATCH: Initialize gesture.maxRatio to enable pinch zoom after programmatic zoom
+    // Without this, pinch gestures after double-tap won't work properly
+    gesture.maxRatio = getMaxRatio();
+
     if (swiper.params.cssMode) {
       swiper.wrapperEl.style.overflow = 'hidden';
       swiper.wrapperEl.style.touchAction = 'none';
@@ -561,6 +566,7 @@ function Zoom({
       }
     }
     if (!gesture.imageEl || !gesture.imageWrapEl) return;
+    gesture.maxRatio = getMaxRatio();
     if (swiper.params.cssMode) {
       swiper.wrapperEl.style.overflow = '';
       swiper.wrapperEl.style.touchAction = '';
